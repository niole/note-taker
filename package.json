{
  "name": "notetaker",
  "version": "1.0.0",
  "description": "Chrome extension that records sources while user takes notes",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "./build/watch.sh",
    "build": "./build/build.sh",
    "build-elm": "elm make src/elm/Main.elm --output=output/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "elm": "^0.19.0-bugfix2"
  },
  "devDependencies": {
    "chokidar-cli": "^1.2.1"
  }
}
